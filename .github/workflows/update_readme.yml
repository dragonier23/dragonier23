name: Update README with XKCD

on:
  schedule:
    - cron: "1 0 * * *" # every day at midnight UTC
  workflow_dispatch:
  push: 

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Update README
        run: npm run update_readme

      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "chore: update XKCD" || echo "No changes to commit"
          git push
